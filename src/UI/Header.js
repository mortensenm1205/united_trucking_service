import React from 'react';

const Header = () => {
  return(
    <header>
      <a href="#">Home</a>
      <a href="#about">About</a>
      <a href="#contact">Contact</a>
    </header>
  )
}

export default Header;
